$(document).ready(function(){
    $('.flat__desc-slider').slick({
        arrows : false,
        dots : true,
        fade : true
    });
  });
      
      
    //   $('.fade').slick({
    // dots: true,
    // infinite: true,
    // speed: 500,
    // fade: true,
    // cssEase: 'linear'
